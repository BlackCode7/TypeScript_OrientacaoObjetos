// A boolean represents the most basic datatype in TypeScript, with the purpose of assigning true/false values.
// Um booleano representa o tipo de dado mais básico em TypeScript, com a finalidade de atribuir valores verdadeiro/falso.

// set initial values
let isTrue: boolean = true;
let unsetBool: boolean;
let nullableBool: boolean = null;

console.log(isTrue, unsetBool, nullableBool);